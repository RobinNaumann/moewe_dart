name: moewe
description: a client library for mœwe
version: 0.2.0
repository: https://github.com/RobinNaumann/moewe_dart

environment:
  sdk: ^3.1.4

# Add regular dependencies here.
dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  url_launcher: ^6.2.4
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
